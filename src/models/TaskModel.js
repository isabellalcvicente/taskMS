class TaskModel{
    constructor(title, description, created_at, status){
        this.title=title,
        this.description=description,
        this.created_at= created_at,
        this.status=status 
    }
}

export default TaskModel